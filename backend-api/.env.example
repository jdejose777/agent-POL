# Variables de entorno para la API RAG
# Backend FastAPI con Vertex AI

# Clave de API de Pinecone
PINECONE_API_KEY=your_pinecone_api_key_here

# Nombre del índice de Pinecone (1024 dimensiones para Vertex AI)
PINECONE_INDEX_NAME=codigo-penal-vertex-ai

# Google Cloud Configuration (Vertex AI)
GOOGLE_APPLICATION_CREDENTIALS=/path/to/your/service-account-key.json
GCP_PROJECT_ID=your-gcp-project-id
GCP_REGION=us-central1

# Google Generative Language API Key (Gemini)
GEMINI_API_KEY=your_gemini_api_key_here

# 🗄️ Redis Configuration (MEJORA #9: Cache persistente)
# Host donde corre Redis (localhost para desarrollo local)
REDIS_HOST=localhost

# Puerto de Redis (por defecto 6379)
REDIS_PORT=6379

# Número de base de datos Redis (0-15, por defecto 0)
REDIS_DB=0

# TTL (Time To Live) del caché en segundos (86400 = 24 horas)
REDIS_TTL=86400
